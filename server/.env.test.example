# =============================================================================
# AUTHENTICATION SECURITY TESTS - READ-ONLY CONFIGURATION
# =============================================================================
#
# These tests verify authentication boundaries WITHOUT modifying the database.
# All tests use GET endpoints only to check if tokens are accepted/rejected.
#
# REQUIRED: Admin credentials for generating JWT and API key
# OPTIONAL: Keystone JWT for testing external authentication
#
# Copy this file to .env.test and fill in your credentials
# =============================================================================

# Server Configuration
TEST_SERVER_URL=http://localhost:3001

# Admin Credentials (REQUIRED)
# Used to login and generate JWT → API key for testing
TEST_ADMIN_USERNAME=your-admin-username
TEST_ADMIN_PASSWORD=your-admin-password

# Keystone JWT (OPTIONAL)
# If provided, tests will verify Keystone JWT authentication
# If not provided, Keystone tests will be skipped
TEST_KEYSTONE_JWT=http://localhost:3001

# External auth configuration (should match your main .env)
EXTERNAL_AUTH_ENABLED=true
EXTERNAL_AUTH_MODE=introspect
EXTERNAL_AUTH_API_URL=http://localhost:3000
EXTERNAL_AUTH_ISSUER=your-issuer
EXTERNAL_AUTH_AUDIENCE=your-audience
EXTERNAL_API_SERVICE_KEY=your-service-key

# JWT Secret (should match your main .env)
JWT_SECRET=your-jwt-secret

###########################################
######## Performance Test Settings ########
###########################################

# Number of concurrent requests for stress testing
STRESS_TEST_CONCURRENT_REQUESTS=50

# Number of total requests per test
STRESS_TEST_TOTAL_REQUESTS=1000

# Request timeout in milliseconds
STRESS_TEST_TIMEOUT_MS=5000
